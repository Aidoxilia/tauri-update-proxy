{
  "name": "tauri-update-proxy",
  "version": "1.0.0",
  "description": "Proxy server to serve Tauri auto-updates from a private GitHub repo",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "semver": "^7.8.4"
  },
  "engines": {
    "node": ">=18"
  }
}
